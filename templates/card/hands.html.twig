{# templates/index.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Visualizing some cards!{% endblock %}
{% block body %}
<main class="mt-3">
  <section>
    <h1>Here are some hands</h1>
    <p>
      Cards left in deck: {{deck.getNumberOfCards()}}
    </p>
    {% for hand in hands %}
    <li>Player {{loop.index}}
      {% for card in hand.getHand() %}
        {{dump(card.getCard())}}
      {% endfor %}
    </li>
    {% endfor %}
  </section>
</main>
{% endblock %}